import { AdminLayout } from "@/components/admin-layout";

/**
 * Render the Users admin page inside the AdminLayout.
 *
 * @returns {JSX.Element} A JSX element containing AdminLayout configured with title "Users".
 */
export default function Users() {
  return <AdminLayout title="Users"></AdminLayout>;
}